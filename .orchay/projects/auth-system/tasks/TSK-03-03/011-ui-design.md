# TSK-03-03 - usePermission Hook 및 화면 요소 제어 UI 설계 문서

## 문서 정보

| 항목 | 내용 |
|------|------|
| Task ID | TSK-03-03 |
| 문서 버전 | 1.0 |
| 작성일 | 2026-01-27 |

---

## 1. 개요

### 1.1 목적

권한 기반으로 화면 요소(버튼, 필드, 영역)를 제어하는 React Hook과 Wrapper 컴포넌트의 UI/UX 설계

### 1.2 주요 기능

- `usePermission` Hook: 권한 체크 유틸리티
- `PermissionGuard` 컴포넌트: 조건부 렌더링 Wrapper
- 권한별 UI 요소 표시/숨김/비활성화

---

## 2. 권한 제어 패턴

### 2.1 UI 제어 방식

| 제어 방식 | 설명 | 사용 케이스 |
|----------|------|------------|
| **숨김 (Hidden)** | 권한 없으면 렌더링 안 함 | 메뉴, 버튼, 영역 |
| **비활성화 (Disabled)** | 렌더링하되 클릭 불가 | 버튼, 입력 필드 |
| **읽기 전용 (ReadOnly)** | 보기만 가능 | 입력 필드, 폼 |
| **대체 (Fallback)** | 다른 UI로 대체 | 제한된 정보 표시 |

### 2.2 패턴별 시각적 표현

#### 2.2.1 숨김 패턴

```
권한 있음                    권한 없음
┌────────────────────┐      ┌────────────────────┐
│ [등록] [수정] [삭제]│      │ [등록] [수정]      │
└────────────────────┘      └────────────────────┘
                            (삭제 버튼 숨김)
```

#### 2.2.2 비활성화 패턴

```
권한 있음                    권한 없음
┌────────────────────┐      ┌────────────────────┐
│ [등록] [수정] [삭제]│      │ [등록] [수정] [삭제]│
└────────────────────┘      └────────────────────┘
                            (삭제 버튼 회색, 클릭 불가)
```

#### 2.2.3 읽기 전용 패턴

```
권한 있음                    권한 없음
┌────────────────────┐      ┌────────────────────┐
│ 이름: [홍길동     ]│      │ 이름: 홍길동        │
│ 이메일: [hong@...]│      │ 이메일: hong@...    │
│ [저장] [취소]      │      │ (편집 불가)         │
└────────────────────┘      └────────────────────┘
```

---

## 3. 컴포넌트 API 설계

### 3.1 usePermission Hook

```typescript
interface UsePermissionReturn {
  // 권한 체크 함수
  can: (action: string, subject: string) => boolean
  cannot: (action: string, subject: string) => boolean

  // 권한 목록
  permissions: string[]
  roles: Role[]

  // 로딩 상태
  isLoading: boolean
}

// 사용 예시
const { can, cannot } = usePermission()

// 단일 권한 체크
can('create', 'User')  // user:create 권한 있으면 true
can('delete', 'Role')  // role:delete 권한 있으면 true

// 복합 권한 체크
can('manage', 'all')   // 모든 권한 (SYSTEM_ADMIN)
```

### 3.2 PermissionGuard 컴포넌트

```typescript
interface PermissionGuardProps {
  // 필수: 권한 조건
  action: string
  subject: string

  // 선택: 대체 표시
  fallback?: React.ReactNode

  // 선택: 숨김 대신 비활성화
  disableOnly?: boolean

  // 자식 요소
  children: React.ReactNode
}
```

---

## 4. 사용 패턴별 UI 예시

### 4.1 버튼 숨김 패턴

```
사용자 관리

총 123명                                      [+ 사용자 등록]  ← 권한 있을 때만 표시

┌─────────────────────────────────────────────────────────────────┐
│ 이름     │ 이메일           │ 역할     │ 상태  │ 액션          │
├─────────┼─────────────────┼─────────┼──────┼──────────────┤
│ 홍길동   │ hong@test.com  │ 관리자   │ 활성  │ [⋮]          │
│ 김철수   │ kim@test.com   │ 사용자   │ 활성  │ [⋮]          │
└─────────────────────────────────────────────────────────────────┘

액션 메뉴 (권한별 표시)
┌────────────────────┐
│ ✏️ 수정            │  ← user:update 권한 있을 때
│ 👥 역할 할당       │  ← user:assign-role 권한 있을 때
│ ──────────────── │
│ 🔒 계정 잠금       │  ← user:lock 권한 있을 때
│ 🔑 비밀번호 초기화 │  ← user:password-reset 권한 있을 때
│ ──────────────── │
│ 🗑️ 삭제           │  ← user:delete 권한 있을 때
└────────────────────┘
```

### 4.2 영역 숨김 패턴

```
대시보드

┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 일반 통계     │  │ 생산 현황    │  │ 품질 현황    │          │
│  │              │  │              │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                 │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ 감사 로그 (audit-log:read 권한 있을 때만 표시)               ││
│  │                                                            ││
│  │ - 최근 로그인 이력                                          ││
│  │ - 권한 변경 이력                                            ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 읽기 전용 패턴 (폼)

```
사용자 상세

┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  이름          ┌────────────────────────────────────┐          │
│               │ 홍길동                              │          │
│               └────────────────────────────────────┘          │
│               ↑ user:update 없으면 Input → Text 변환           │
│                                                                 │
│  이메일        ┌────────────────────────────────────┐          │
│               │ hong@example.com                   │          │
│               └────────────────────────────────────┘          │
│                                                                 │
│  역할          ┌────────────────────────────────────┐          │
│               │ [시스템 관리자] [일반 사용자]        │          │
│               └────────────────────────────────────┘          │
│               ↑ user:assign-role 없으면 Select → Tag 변환      │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 권한 없음: 수정 권한이 없습니다.                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.4 비활성화 패턴

```
역할 관리

┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  역할 목록                                [+ 역할 등록]          │
│                                           ↑ 비활성화 (회색)      │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ ▶ 시스템 관리자 (SYSTEM_ADMIN)           [수정] [삭제]  │    │
│  │   ├─ 보안 관리자 (SECURITY_ADMIN)        [수정] [삭제]  │    │
│  │   └─ 운영 관리자 (OPERATION_ADMIN)       [수정] [삭제]  │    │
│  │                                          ↑ 비활성화     │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  [삭제] 버튼은 role:delete 권한 없으면 비활성화 + 툴팁 표시     │
│  툴팁: "역할 삭제 권한이 없습니다"                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 5. 코드 사용 예시

### 5.1 버튼 숨김

```tsx
function UserManagement() {
  const { can } = usePermission()

  return (
    <Space>
      {can('create', 'User') && (
        <Button type="primary" icon={<PlusOutlined />}>
          사용자 등록
        </Button>
      )}
    </Space>
  )
}
```

### 5.2 PermissionGuard 사용

```tsx
function UserManagement() {
  return (
    <>
      {/* 숨김 패턴 */}
      <PermissionGuard action="create" subject="User">
        <Button type="primary">등록</Button>
      </PermissionGuard>

      {/* 비활성화 패턴 */}
      <PermissionGuard action="delete" subject="User" disableOnly>
        <Button danger>삭제</Button>
      </PermissionGuard>

      {/* 대체 표시 패턴 */}
      <PermissionGuard
        action="read"
        subject="AuditLog"
        fallback={<Alert message="권한이 없습니다" type="warning" />}
      >
        <AuditLogTable />
      </PermissionGuard>
    </>
  )
}
```

### 5.3 폼 필드 제어

```tsx
function UserForm({ user }) {
  const { can } = usePermission()
  const canEdit = can('update', 'User')

  return (
    <Form>
      <Form.Item label="이름">
        {canEdit ? (
          <Input defaultValue={user.name} />
        ) : (
          <Typography.Text>{user.name}</Typography.Text>
        )}
      </Form.Item>

      <Form.Item label="역할">
        {can('assign-role', 'User') ? (
          <Select mode="multiple" defaultValue={user.roleIds}>
            {roles.map(r => <Option key={r.id}>{r.name}</Option>)}
          </Select>
        ) : (
          <Space>
            {user.roles.map(r => <Tag key={r.id}>{r.name}</Tag>)}
          </Space>
        )}
      </Form.Item>
    </Form>
  )
}
```

---

## 6. 시각적 피드백

### 6.1 비활성화 버튼 스타일

```
[버튼 비활성화 상태]
┌─────────────────┐
│ 🗑️ 삭제        │  ← 회색 배경, 회색 텍스트
└─────────────────┘    cursor: not-allowed

툴팁 (hover 시)
┌─────────────────────────────────┐
│ 이 작업을 수행할 권한이 없습니다. │
└─────────────────────────────────┘
```

### 6.2 권한 부족 알림

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ 권한 부족                                                    │
│                                                                 │
│ 이 기능에 접근하려면 다음 권한이 필요합니다:                      │
│ • user:delete (사용자 삭제)                                     │
│                                                                 │
│ 관리자에게 문의하세요.                                           │
│                                                        [확인]   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 권한 매핑 테이블

### 7.1 사용자 관리 화면

| UI 요소 | 필요 권한 | 제어 방식 |
|--------|----------|----------|
| 등록 버튼 | user:create | 숨김 |
| 수정 버튼 | user:update | 숨김 |
| 삭제 버튼 | user:delete | 숨김 |
| 역할 할당 | user:assign-role | 숨김 |
| 계정 잠금 | user:lock | 숨김 |
| 비밀번호 초기화 | user:password-reset | 숨김 |

### 7.2 역할 관리 화면

| UI 요소 | 필요 권한 | 제어 방식 |
|--------|----------|----------|
| 등록 버튼 | role:create | 숨김 |
| 수정 버튼 | role:update | 숨김 |
| 삭제 버튼 | role:delete | 비활성화 |
| 권한 매핑 | role:assign-permission | 숨김 |

### 7.3 감사 로그 화면

| UI 요소 | 필요 권한 | 제어 방식 |
|--------|----------|----------|
| 전체 영역 | audit-log:read | 숨김 |
| 내보내기 버튼 | audit-log:export | 숨김 |

---

## 변경 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0 | 2026-01-27 | Claude | 최초 작성 |
