# TSK-02-03 - 탭 드래그 앤 드롭 UI 설계

**Version:** 1.0 — **Last Updated:** 2026-01-20

> **목적**: MDI 탭 바에서 탭을 드래그하여 순서를 변경하는 기능의 UI 설계

---

## 1. 화면 목록

| 화면 ID | 화면명 | 목적 | SVG 참조 |
|---------|--------|------|----------|
| SCR-01 | 탭 바 (기본 상태) | 탭 목록 기본 표시 상태 | `screen-01-tabbar-default.svg` |
| SCR-02 | 탭 바 (드래그 시작) | 탭 드래그 시작 시 시각적 피드백 | `screen-02-tabbar-drag-start.svg` |
| SCR-03 | 탭 바 (드래그 중) | 드래그 중 드롭 인디케이터 표시 | `screen-03-tabbar-dragging.svg` |
| SCR-04 | 탭 바 (드롭 완료) | 탭 순서 변경 완료 상태 | `screen-04-tabbar-drop-complete.svg` |
| SCR-05 | 탭 바 (영역 밖 드래그) | 유효하지 않은 드롭 영역 표시 | `screen-05-tabbar-invalid-drop.svg` |

---

## 2. 화면 전환 흐름

### 2.1 상태 다이어그램

```mermaid
stateDiagram-v2
    [*] --> Default: 초기화

    Default --> DragStart: mousedown on tab
    DragStart --> Dragging: mousemove
    Dragging --> Dragging: mousemove (위치 업데이트)
    Dragging --> DropComplete: mouseup (유효 위치)
    Dragging --> Default: mouseup (무효 위치)
    Dragging --> Default: ESC key

    DropComplete --> Default: 애니메이션 완료

    note right of DragStart
        탭 반투명화
        커서 grabbing
    end note

    note right of Dragging
        드롭 인디케이터 표시
        고스트 탭 마우스 따라감
    end note

    note right of DropComplete
        탭 순서 업데이트
        부드러운 전환
    end note
```

### 2.2 액션-화면 매트릭스

| 액션 | 현재 상태 | 결과 상태 | 트리거 | 비고 |
|------|----------|----------|--------|------|
| 탭 클릭 홀드 | Default | DragStart | mousedown | 150ms 딜레이 없이 즉시 |
| 마우스 이동 | DragStart | Dragging | mousemove | 드래그 중 상태 |
| 다른 탭 위 이동 | Dragging | Dragging | mousemove | 드롭 인디케이터 위치 업데이트 |
| 마우스 놓기 (탭 위) | Dragging | DropComplete | mouseup | 순서 변경 |
| 마우스 놓기 (탭 밖) | Dragging | Default | mouseup | 취소, 원래 위치 |
| ESC 키 | Dragging | Default | keydown | 드래그 취소 |

---

## 3. 화면별 상세

### 3.1 SCR-01: 탭 바 (기본 상태)

**화면 목적**: MDI 탭 바의 기본 상태로 탭 목록을 표시

**레이아웃 구조**:
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐                │
│  │ 📊 대시보드      │ │ 📋 작업지시   ✕  │ │ 📈 생산현황   ✕  │                │
│  │   (활성)        │ │                 │ │                 │                │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘                │
│     ▲ 하단 하이라이트                                                        │
└─────────────────────────────────────────────────────────────────────────────┘
                          높이: 40px (--tab-bar-height)
```

**컴포넌트 구성**:

| 컴포넌트 | 역할 | Props | 비고 |
|----------|------|-------|------|
| `TabBar` | 탭 바 컨테이너 | `tabs`, `activeTabId` | DndContext 래핑 |
| `SortableTabItem` | 드래그 가능한 탭 | `tab`, `isActive` | useSortable 적용 |

**사용자 액션**:

| 액션 | 요소 | 커서 | 결과 |
|------|------|------|------|
| 호버 | 탭 아이템 | `grab` | 배경색 변화 |
| 클릭 | 탭 아이템 | `pointer` | 탭 전환 |
| 드래그 시작 | 탭 아이템 | `grabbing` | 드래그 상태 전환 |

---

### 3.2 SCR-02: 탭 바 (드래그 시작)

**화면 목적**: 사용자가 탭을 드래그하기 시작했을 때의 시각적 피드백

**레이아웃 구조**:
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐ ╔═════════════════╗ ┌─────────────────┐                │
│  │ 📊 대시보드      │ ║ 📋 작업지시   ✕  ║ │ 📈 생산현황   ✕  │                │
│  │   (활성)        │ ║   (드래그 중)   ║ │                 │                │
│  └─────────────────┘ ╚═════════════════╝ └─────────────────┘                │
│                         ↑ 반투명 (opacity: 0.5)                              │
│                         ↑ 테두리 강조 (border: dashed blue)                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**시각적 변화**:

| 요소 | 기본 상태 | 드래그 시작 상태 |
|------|----------|-----------------|
| 드래그 탭 | `opacity: 1` | `opacity: 0.5` |
| 드래그 탭 테두리 | `none` | `2px dashed #1677ff` |
| 커서 | `grab` | `grabbing` |

**컴포넌트 상태**:
```typescript
// @dnd-kit DragOverlay로 고스트 생성
{activeId && (
  <DragOverlay>
    <TabItem tab={activeTab} isDragOverlay />
  </DragOverlay>
)}
```

---

### 3.3 SCR-03: 탭 바 (드래그 중)

**화면 목적**: 드래그 중 드롭 위치를 시각적으로 표시

**레이아웃 구조**:
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐ │ ┌ ─ ─ ─ ─ ─ ─ ─ ─┐ ┌─────────────────┐              │
│  │ 📊 대시보드      │ │ │ placeholder    │ │ 📈 생산현황   ✕  │              │
│  │   (활성)        │ │ └ ─ ─ ─ ─ ─ ─ ─ ─┘ │                 │              │
│  └─────────────────┘ │                     └─────────────────┘              │
│                      ↑                                                       │
│              드롭 인디케이터 (3px solid #1677ff)                              │
│                                                                              │
│         ┌─────────────────┐                                                  │
│         │ 📋 작업지시   ✕  │  ← 고스트 탭 (마우스 따라다님)                   │
│         │   (shadow)      │     box-shadow, opacity: 0.8                     │
│         └─────────────────┘                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**컴포넌트 구성**:

| 컴포넌트 | 역할 | 스타일 |
|----------|------|--------|
| 원래 탭 위치 | Placeholder | `opacity: 0`, 공간 유지 |
| 드롭 인디케이터 | 삽입 위치 표시 | `3px solid #1677ff`, 세로 라인 |
| 드래그 오버레이 | 마우스 따라다니는 탭 | `box-shadow`, `opacity: 0.8` |

**드롭 인디케이터 위치 계산**:
```typescript
// 드롭 인디케이터는 호버된 탭의 좌측 또는 우측에 표시
// activeId 탭의 원래 위치 기준으로 좌/우 결정
const indicatorPosition = getIndicatorPosition(activeIndex, overIndex);
// activeIndex < overIndex → 우측에 표시
// activeIndex > overIndex → 좌측에 표시
```

---

### 3.4 SCR-04: 탭 바 (드롭 완료)

**화면 목적**: 탭 순서가 변경된 후의 상태

**레이아웃 구조**:
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐                │
│  │ 📊 대시보드      │ │ 📈 생산현황   ✕  │ │ 📋 작업지시   ✕  │                │
│  │   (활성)        │ │                 │ │                 │                │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘                │
│                                                                              │
│  ✓ 작업지시 탭이 생산현황 뒤로 이동됨 (transition: 200ms ease)              │
└─────────────────────────────────────────────────────────────────────────────┘
```

**애니메이션**:

| 요소 | 속성 | 값 |
|------|------|-----|
| 탭 이동 | `transition` | `transform 200ms ease` |
| 드롭 탭 | `transform` | `scale(1)` (고스트에서 복귀) |

**상태 업데이트**:
```typescript
// reorderTabs 호출로 MDI Context 상태 업데이트
const handleDragEnd = (event: DragEndEvent) => {
  const { active, over } = event;

  if (over && active.id !== over.id) {
    reorderTabs(active.id as string, over.id as string);
  }

  setActiveId(null); // 드래그 상태 초기화
};
```

---

### 3.5 SCR-05: 탭 바 (영역 밖 드래그)

**화면 목적**: 탭 바 영역 밖으로 드래그할 때의 시각적 피드백

**레이아웃 구조**:
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────────┐ ┌ ─ ─ ─ ─ ─ ─ ─ ─┐ ┌─────────────────┐                │
│  │ 📊 대시보드      │ │                 │ │ 📈 생산현황   ✕  │                │
│  │   (활성)        │ └ ─ ─ ─ ─ ─ ─ ─ ─┘ │                 │                │
│  └─────────────────┘                     └─────────────────┘                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                                     🚫                                       │
│                    ┌─────────────────┐                                       │
│                    │ 📋 작업지시   ✕  │  ← 드롭 불가 영역                     │
│                    │   (red border) │     cursor: not-allowed               │
│                    └─────────────────┘                                       │
│                                                                              │
│                        컨텐츠 영역 (드롭 불가)                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

**시각적 피드백**:

| 요소 | 상태 | 스타일 |
|------|------|--------|
| 고스트 탭 | 유효하지 않은 영역 | `border: 2px solid #ff4d4f`, `opacity: 0.5` |
| 커서 | 드롭 불가 | `not-allowed` |
| 드롭 인디케이터 | 숨김 | 표시 안 함 |

**동작**:
- 탭 바 영역 밖에서 마우스를 놓으면 드래그 취소
- 탭은 원래 위치로 복귀

---

## 4. 공통 컴포넌트

### 4.1 SortableTabItem

@dnd-kit의 useSortable을 적용한 드래그 가능한 탭 아이템

```typescript
interface SortableTabItemProps {
  tab: Tab;
  isActive: boolean;
  onClose: (e: React.MouseEvent) => void;
}

const SortableTabItem: React.FC<SortableTabItemProps> = ({ tab, isActive, onClose }) => {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id: tab.id });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
  };

  return (
    <div ref={setNodeRef} style={style} {...attributes} {...listeners}>
      <TabItem tab={tab} isActive={isActive} onClose={onClose} />
    </div>
  );
};
```

### 4.2 DropIndicator

드롭 위치를 표시하는 시각적 인디케이터

```typescript
interface DropIndicatorProps {
  position: 'left' | 'right';
  visible: boolean;
}

const DropIndicator: React.FC<DropIndicatorProps> = ({ position, visible }) => (
  <div
    className={`
      absolute top-0 h-full w-[3px] bg-blue-500
      ${position === 'left' ? 'left-0' : 'right-0'}
      ${visible ? 'opacity-100' : 'opacity-0'}
      transition-opacity duration-150
    `}
  />
);
```

### 4.3 DragOverlay 탭

드래그 중 마우스를 따라다니는 탭 복제본

```typescript
<DragOverlay dropAnimation={{
  duration: 200,
  easing: 'cubic-bezier(0.18, 0.67, 0.6, 1.22)',
}}>
  {activeId && (
    <TabItem
      tab={getTabById(activeId)}
      isDragOverlay
      className="shadow-lg opacity-90"
    />
  )}
</DragOverlay>
```

---

## 5. 반응형 설계

### 5.1 Breakpoint 정의

| Breakpoint | 너비 범위 | 드래그 앤 드롭 동작 |
|------------|----------|-------------------|
| Desktop | 1024px+ | 완전 지원 (마우스 드래그) |
| Tablet | 768-1023px | 터치 드래그 지원 (길게 누르기) |
| Mobile | 0-767px | 드래그 비활성화 (탭 선택만) |

### 5.2 터치 디바이스 지원 (Tablet)

```typescript
// @dnd-kit 터치 센서 추가
import { TouchSensor, useSensor, useSensors } from '@dnd-kit/core';

const touchSensor = useSensor(TouchSensor, {
  activationConstraint: {
    delay: 250,      // 250ms 길게 누르기
    tolerance: 5,    // 5px 이동 허용
  },
});

const sensors = useSensors(mouseSensor, touchSensor);
```

### 5.3 모바일 비활성화

```typescript
// 모바일에서 드래그 비활성화
const isMobile = useMediaQuery('(max-width: 767px)');

<DndContext sensors={isMobile ? [] : sensors}>
  {/* ... */}
</DndContext>
```

---

## 6. 접근성

### 6.1 키보드 네비게이션

| 키 | 동작 | 조건 |
|-----|------|------|
| Space/Enter | 드래그 시작/종료 | 탭에 포커스 시 |
| Arrow Left/Right | 드래그 중 위치 이동 | 드래그 활성화 시 |
| Escape | 드래그 취소 | 드래그 중 |

### 6.2 ARIA 속성

| 요소 | ARIA 속성 | 값 |
|------|----------|-----|
| TabBar | `role` | `tablist` |
| SortableTabItem | `role` | `tab` |
| SortableTabItem | `aria-selected` | `{isActive}` |
| SortableTabItem | `aria-grabbed` | `{isDragging}` |
| SortableTabItem | `aria-describedby` | `dnd-instructions` |

### 6.3 스크린 리더 안내

```typescript
<div id="dnd-instructions" className="sr-only">
  탭을 드래그하여 순서를 변경할 수 있습니다.
  스페이스 또는 엔터로 드래그를 시작하고,
  화살표 키로 이동한 후 다시 스페이스 또는 엔터로 드롭합니다.
  ESC로 취소할 수 있습니다.
</div>
```

### 6.4 색상 대비

| 요소 | 색상 | 배경 | 대비율 |
|------|------|------|--------|
| 드롭 인디케이터 | #1677ff | - | 고대비 단독 요소 |
| 드래그 탭 테두리 | #1677ff | 흰색 | 4.6:1 |
| 무효 영역 테두리 | #ff4d4f | 흰색 | 5.2:1 |

---

## 7. SVG 파일 목록

| 파일명 | 설명 | 뷰포트 |
|--------|------|--------|
| `screen-01-tabbar-default.svg` | 탭 바 기본 상태 | 1280x100 |
| `screen-02-tabbar-drag-start.svg` | 드래그 시작 상태 | 1280x100 |
| `screen-03-tabbar-dragging.svg` | 드래그 중 상태 | 1280x200 |
| `screen-04-tabbar-drop-complete.svg` | 드롭 완료 상태 | 1280x100 |
| `screen-05-tabbar-invalid-drop.svg` | 영역 밖 드래그 | 1280x300 |

---

## 8. 컴포넌트 구조

```
components/
└── mdi/
    ├── TabBar.tsx              # 탭 바 컨테이너 (DndContext 래핑)
    ├── TabItem.tsx             # 기본 탭 아이템 UI
    ├── SortableTabItem.tsx     # 드래그 가능한 탭 아이템
    └── index.ts                # 내보내기

lib/
└── mdi/
    ├── context.tsx             # MDI Context (reorderTabs 포함)
    └── types.ts                # Tab 타입 정의
```

---

## 관련 문서

- 설계: `010-design.md`
- 테스트 명세: `026-test-specification.md`
- 추적성 매트릭스: `025-traceability-matrix.md`

---

<!--
TSK-02-03 UI Design
Version: 1.0
Created: 2026-01-20
-->
