# TSK-02-01 UI 설계서 - 사용자 역할그룹 할당 화면

## 1. 와이어프레임

### 1.1 전체 레이아웃 (3-column)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  PageHeader: 사용자 권한 할당                                                │
├───────────────┬─────────────────────────┬───────────────────────────────────┤
│   30%         │       35%               │         35%                       │
│               │                         │                                   │
│  ┌──────────┐ │  ┌───────────────────┐  │  ┌─────────────────────────────┐  │
│  │🔍 검색   │ │  │ Card: 보유 역할그룹│  │  │ Card: 메뉴 시뮬레이션       │  │
│  │[이름/이메│ │  │                   │  │  │                             │  │
│  │일 검색  ]│ │  │ ┌───────────────┐ │  │  │  📁 생산관리                │  │
│  ├──────────┤ │  │ │ Table         │ │  │  │   ├─ 📄 작업지시            │  │
│  │[상태 ▼ ]│ │  │ │ (read-only)   │ │  │  │   ├─ 📄 생산현황            │  │
│  ├──────────┤ │  │ └───────────────┘ │  │  │   └─ 📄 실적등록            │  │
│  │          │ │  └───────────────────┘  │  │  📁 품질관리                │  │
│  │ Table    │ │                         │  │   ├─ 📄 검사관리            │  │
│  │          │ │  ┌───────────────────┐  │  │   └─ 📄 불량관리            │  │
│  │ ● 홍길동 │ │  │ Card: 전체 역할그룹│  │  │  📁 설비관리                │  │
│  │   길동@.. │ │  │                   │  │  │   └─ 📄 설비현황            │  │
│  │   활성   │ │  │ ┌───────────────┐ │  │  │                             │  │
│  │          │ │  │ │ ☑ Table       │ │  │  ├─────────────────────────────┤  │
│  │   이몽룡 │ │  │ │ (checkbox)    │ │  │  │ 접근 가능 메뉴: 8개 메뉴   │  │
│  │   몽룡@.. │ │  │ └───────────────┘ │  │  │ / 3개 카테고리              │  │
│  │   비활성 │ │  │                   │  │  └─────────────────────────────┘  │
│  │          │ │  │    [💾 저장]      │  │                                   │
│  └──────────┘ │  └───────────────────┘  │                                   │
└───────────────┴─────────────────────────┴───────────────────────────────────┘
```

### 1.2 사용자 미선택 상태

```
┌───────────────┬─────────────────────────┬───────────────────────────────────┐
│  사용자 목록   │                         │                                   │
│               │   ┌─────────────────┐   │   ┌─────────────────────────┐     │
│  (정상 표시)  │   │                 │   │   │                         │     │
│               │   │  📋             │   │   │  📋                     │     │
│               │   │  사용자를        │   │   │  사용자를                │     │
│               │   │  선택해주세요    │   │   │  선택해주세요            │     │
│               │   │                 │   │   │                         │     │
│               │   └─────────────────┘   │   └─────────────────────────┘     │
└───────────────┴─────────────────────────┴───────────────────────────────────┘
```

### 1.3 미저장 변경사항 경고 Modal

```
┌──────────────────────────────────┐
│  ⚠️ 미저장 변경사항               │
│                                  │
│  변경사항이 저장되지 않았습니다.   │
│  다른 사용자로 전환하시겠습니까?   │
│                                  │
│         [취소]     [전환]         │
└──────────────────────────────────┘
```

## 2. 상태 전이도

```
                    ┌──────────────┐
                    │ 초기 상태     │
                    │ (미선택)      │
                    └──────┬───────┘
                           │ 사용자 행 클릭
                           ▼
                    ┌──────────────┐
          ┌────────│ 사용자 선택됨  │◄─────────────────────────────┐
          │        │ (로딩 중)     │                               │
          │        └──────┬───────┘                               │
          │               │ API 응답 완료                          │
          │               ▼                                       │
          │        ┌──────────────┐                               │
          │        │ 데이터 표시   │─── 체크박스 변경 ──┐          │
          │        │ (clean)      │                   │          │
          │        └──────┬───────┘                   ▼          │
          │               │                    ┌──────────────┐  │
          │               │ 체크박스 변경       │ 메뉴 갱신     │  │
          │               ▼                    │ (debounce     │  │
          │        ┌──────────────┐            │  300ms)       │  │
          │        │ 데이터 표시   │◄───────────└──────────────┘  │
          │        │ (dirty)      │                               │
          │        └──────┬───────┘                               │
          │               │                                       │
          │      ┌────────┴────────┐                              │
          │      │                 │                               │
          │      ▼                 ▼                               │
          │ [저장 클릭]      [다른 사용자 클릭]                     │
          │      │                 │                               │
          │      ▼                 ▼                               │
          │ PUT API 호출    ┌──────────────┐                      │
          │      │         │ Modal.confirm │                      │
          │      ▼         │ 미저장 경고    │                      │
          │ 성공 → clean   └──────┬───────┘                      │
          │                      │                               │
          │              ┌───────┴───────┐                       │
          │              │               │                       │
          │           [취소]          [전환]                      │
          │              │               │                       │
          │              ▼               └───────────────────────┘
          │         현재 유지
          │
          │ (변경 없이 다른 사용자 클릭)
          └───────────────────────────────────────────────────────┘
```

### 상태 요약

| 상태 | selectedUser | isDirty | 중앙 패널 | 우측 패널 |
|------|-------------|---------|----------|----------|
| 초기 (미선택) | `null` | `false` | Empty 안내 | Empty 안내 |
| 로딩 중 | User | `false` | Spin 로딩 | Spin 로딩 |
| 데이터 표시 (clean) | User | `false` | 역할그룹 표시 | 메뉴 트리 표시 |
| 데이터 표시 (dirty) | User | `true` | 역할그룹 표시 (변경됨) | 메뉴 트리 (시뮬레이션) |
| 저장 중 | User | `true` | 저장 버튼 로딩 | 메뉴 트리 표시 |

## 3. Tree 컴포넌트 렌더링 설계

### 3.1 데이터 구조

```typescript
interface MenuTreeNode {
  key: string;          // 메뉴 ID
  title: string;        // 메뉴 표시명
  icon?: string;        // 아이콘 키 (iconMap 참조)
  children?: MenuTreeNode[];
}

interface MenuSimulationResponse {
  menus: MenuTreeNode[];
  summary: {
    totalMenus: number;
    totalCategories: number;
  };
}
```

### 3.2 아이콘 매핑 전략

```typescript
// lib/utils/iconMap.ts에서 공유
// Sidebar.tsx와 동일한 매핑을 사용하여 일관성 보장
import {
  FolderOutlined,
  FileTextOutlined,
  SettingOutlined,
  DashboardOutlined,
  // ... 기타 아이콘
} from '@ant-design/icons';

const iconMap: Record<string, React.ReactNode> = {
  dashboard: <DashboardOutlined />,
  setting: <SettingOutlined />,
  // ... Sidebar.tsx와 동일한 매핑
};

// 카테고리(부모) 노드 기본 아이콘: FolderOutlined
// 메뉴(리프) 노드 기본 아이콘: FileTextOutlined
```

### 3.3 Tree 렌더링 규칙

| 항목 | 설정 |
|------|------|
| `defaultExpandAll` | `true` - 모든 노드 펼침 |
| `showIcon` | `true` - 아이콘 표시 |
| `selectable` | `false` - 클릭해도 선택/네비게이션 없음 (FR-209, BR-203) |
| `blockNode` | `true` - 전체 너비 차지 |
| 아이콘 폴백 | 카테고리 → `FolderOutlined`, 메뉴 → `FileTextOutlined` |

### 3.4 Tree 변환 함수

```typescript
function toTreeData(nodes: MenuTreeNode[]): DataNode[] {
  return nodes.map((node) => ({
    key: node.key,
    title: node.title,
    icon: node.icon ? iconMap[node.icon] : (
      node.children?.length ? <FolderOutlined /> : <FileTextOutlined />
    ),
    children: node.children ? toTreeData(node.children) : undefined,
  }));
}
```

### 3.5 요약 표시

```
──────────────────
접근 가능 메뉴: {totalMenus}개 메뉴 / {totalCategories}개 카테고리
```

- `Divider` 컴포넌트로 구분
- `Typography.Text type="secondary"` 스타일

## 4. 컴포넌트 구조

```
app/(portal)/system/authority/page.tsx
  └─ AuthorityPageContent ('use client')
       ├─ UserListPanel          (좌측 30%)
       │   ├─ Input.Search
       │   ├─ Select (상태 필터)
       │   └─ Table (사용자 목록)
       ├─ RoleGroupPanel         (중앙 35%)
       │   ├─ Card: 보유 역할그룹 Table
       │   ├─ Card: 전체 역할그룹 Table (checkbox)
       │   └─ Button: 저장
       └─ MenuSimulationPanel    (우측 35%)
           ├─ Tree (메뉴 트리)
           └─ Typography.Text (요약)
```

## 5. 반응형 고려사항

- 최소 너비: `1200px` (3-column 유지)
- 각 패널 높이: `calc(100vh - header - page-padding)` → 내부 스크롤
- Table/Tree 영역: `overflow-y: auto`
